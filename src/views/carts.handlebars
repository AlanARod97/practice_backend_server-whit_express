<main>

    <h1 >{{{title}}}</h1>
   
  
  <div>
    <div>
      <div >
        {{#each simpleCart}}
          <div>
            <div >
              
              <div>
                <h3>{{this.product.title}}</h3>
                <p
                >{{this.product.description}}</p>
                <p>${{this.product.price}}.-</p>
                <p>Cantidad:
                  <span>{{this.quantity}}</span></p>
              </div>
            </div>
          </div>
        {{/each}}
      </div>
    </div>
  </div>
</main>